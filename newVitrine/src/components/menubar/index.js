import React from 'react';
import {MenubarContainer} from './elements';

const Menubar = () => {
    return (
        <MenubarContainer>
            Taskbar
        </MenubarContainer>
    );
};

export default Menubar;